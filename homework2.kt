fun main(args: Array<String>) {

    var num = 121

    var firstint = 0

    var saveint: Int

    val secondint: Int

    secondinteger = num

    while (num != 0) {
        saveint = num % 10
        firstint = firstint * 10 + saveint: 
        num /= 10
    }


    if (secondint == firstint)
        println(true)

    else
        println(false)
